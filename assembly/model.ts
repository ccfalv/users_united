//@nearfile
// Exporting a new class PostedMessage so it can be used outside of this file.
export class PostedMessage {
  index: i32;
  id: string;
  name: string;
  text: string;
  date: string;
  photo: string;
  email: string;
}

export class VectorMessage {
  id: string;
  name: string;
  text: string;
  date: string;
  photo: string;
}
